IF (${MY_MYGL_GLAD})
    ADD_DEFINITIONS(-DMY_MYGL_GLAD)
ENDIF ()

GET_TARGET_NAME(CAMERA "${PROJECT_SOURCE_DIR}/src/test/00_basic/camera")
GET_TARGET_NAME(CORE "${PROJECT_SOURCE_DIR}/src/core")
GET_TARGET_NAME(GLAD "${PROJECT_SOURCE_DIR}/src/_deps/glad")
ADD_TARGET(MODE "EXE" TEST ON LIBS "My::${CORE};${GLAD};glfw;${CAMERA}")